#include <iostream>

using namespace std;


int comodines(int *arr[3]);

int main(){

    int rows, cols;
    cin >> rows;
    cin >> cols;
    int** arr = new int*[rows];
    for (int i = 0; i < rows; ++i){
        arr[i] = new int[cols];
        arr[]
    }

    cout << comodines(arr) << endl;

    return 0;
}